# Portrait — Landing Page Preview

Static marketing landing page. Open `index.html`.

Source, the image-generation guide, and tooling live in the working dir
`samples/pricing-sheet/` — **read its `HIGGSFIELD_SETUP.md` before any Higgsfield/image work**,
and edit `portrait-v4.html` there (then copy to `index.html` here) rather than editing `index.html` directly.
